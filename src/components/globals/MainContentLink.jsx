import { Link } from 'next/link';

export default function MainContentLink() {
    return (
            <a href="#main" className="absolute opacity-0 -top-full -left-full focus-visible:top-10 focus-visible:left-10 focus-visible:opacity-100">
                Go to main content
            </a>
    )
} 